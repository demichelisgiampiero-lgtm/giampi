---
tags: [diario]
---

# 2026-05-31

> Voce di diario · collegata a → [[index]] · [[decisions]] · [[profile]]

## In sintesi

Nasce il second brain. Abbiamo costruito da zero la struttura di memoria
persistente per Claude, alla [[karpathy-second-brain|Karpathy]]: file markdown
versionati in git + caricamento automatico all'avvio della sessione.

## Cosa abbiamo fatto

- Creato `CLAUDE.md` con le istruzioni che Claude legge sempre per prime.
- Creato la cartella `memory/` con `profile.md`, `decisions.md`, il diario
  `journal/` (con `_template.md`) e `knowledge/`.
- Scritto l'hook `.claude/hooks/session-start.sh` che a ogni avvio inietta nel
  contesto: profilo, decisioni e le ultime voci di diario.
- Registrato l'hook in `.claude/settings.json` (fuso con la config esistente del
  plugin `claude-mem`, senza sovrascriverla).

## Decisioni prese

- Vedi `../decisions.md` (formato diario, hook sincrono, doppio livello di
  memoria markdown + claude-mem).

## Aperto / da riprendere

- Compilare nel tempo `profile.md` (obiettivi, persone, progetti).
- Iniziare a usare `knowledge/` quando ci saranno argomenti da approfondire.
- Prossima sessione: verificare che l'hook carichi davvero la memoria all'avvio.
